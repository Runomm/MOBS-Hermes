name: hermes
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.11.4

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8

  # Çeviri motoru — offline, ücretsiz, ~30MB/dil
  google_mlkit_translation: ^0.13.0

  # Dil tespiti — offline, ücretsiz, ~3MB bundled model
  # Konuşma Modu'nda çift yönlü routing için (kim Türkçe konuştu, kim İngilizce?)
  google_mlkit_language_id: ^0.13.0

  # OCR — offline, ücretsiz, Latin alfabesi modeli APK'ya gömülü
  # Görsel Çeviri modu (foto/tabela/menü → metin → çeviri)
  # 0.15.x → google_mlkit_commons ^0.11.0 (translation/language_id 0.13 ile uyumlu)
  google_mlkit_text_recognition: ^0.15.0

  # Kamera çekimi + galeri seçimi (Görsel Çeviri)
  image_picker: ^1.1.2

  # Inter font (Hermes UI estetiği)
  google_fonts: ^6.2.1

  # STT — whisper.cpp wrapper, offline, MIT
  whisper_ggml: ^1.4.0

  # Mikrofon kaydı (WAV 16kHz mono için)
  # 6.x: record_linux 0.7.2 + record_platform_interface 1.5.0 uyumsuzluğunu düzeltir
  record: ^6.0.0

  # TTS — Native OS (iOS: AVSpeechSynthesizer, Android: TextToSpeech), offline
  flutter_tts: ^4.2.2

  # VAD — Silero v4/v5 ile sürekli konuşma tespit (Konuşma Modu altyapısı)
  vad: ^0.0.7+1

  # Runtime izinler (mikrofon vb.)
  permission_handler: ^11.3.1

  # Dosya yolu (asset → cihaz dosya sistemi)
  path_provider: ^2.1.5

  # Oturum/mesaj kalıcılığı — type-safe SQLite ORM, oturum geçmişi & transkriptler
  drift: ^2.21.0

  # Drift'in cihazda bundle ettiği SQLite (Android/iOS için sistem libcoderı yerine)
  sqlite3_flutter_libs: ^0.5.24

  # path.join — DB dosya yolu için
  path: ^1.9.0

  # Türkçe ASR (Whisper TR'de zayıf) — offline Vosk, küçük TR modeli (~35MB)
  vosk_flutter_2: ^1.0.5

  # Vosk model indirmede ilerleme takibi (streamed download progress) için
  http: ^1.1.0
  flutter_gemma: ^0.13.6

  # Tier2 çeviri de-risk — NLLB ONNX (seq2seq) on-device inference
  flutter_onnxruntime: ^1.7.1

  # NLLB tokenizer (sentencepiece BPE, saf Dart, HF tokenizer.json/model yükler)
  dart_sentencepiece_tokenizer: ^1.3.1
  shared_preferences: ^2.5.5

  # Cihaz RAM tespiti (F1 — düşük-RAM'de NLLB+ASR eşzamanlı OOM uyarısı).
  # physicalRamSize hem Android hem iOS — bizden native kod istemez.
  device_info_plus: ^11.5.0

  # Canlı çevrimiçi/çevrimdışı tespiti (Home eyebrow rozeti) — event-driven
  # stream, ağ değişince anında günceller (poll'da "takılı kalma" olmaz).
  connectivity_plus: ^6.1.0

  # Liquid-glass UI ikonları — hand-off'taki stroke SVG path'lerini Flutter
  # Path'e çevirir (HgIcon). flutter_svg'den çok daha hafif, saf Dart.
  path_drawing: ^1.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0

  # Drift codegen (app_database.g.dart üretimi)
  drift_dev: ^2.21.0
  build_runner: ^2.4.13

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # Whisper modelleri ve diğer asset'ler
  assets:
    - assets/models/

  # Liquid-glass tasarım dili fontları (hand-off: Manrope + Cormorant Garamond).
  # Variable font'lar — ağırlık seçimi TextStyle.fontVariations ile (HgType).
  fonts:
    - family: Manrope
      fonts:
        - asset: assets/fonts/Manrope-VF.ttf
    - family: CormorantGaramond
      fonts:
        - asset: assets/fonts/CormorantGaramond-VF.ttf
        - asset: assets/fonts/CormorantGaramond-Italic-VF.ttf
          style: italic

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
